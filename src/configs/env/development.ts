// 开发环境配置
const config = {
  // 接口基础路径，如果代理后台接口，那么使用"/api/v1"这种方式
  apiBase: {
    dev1: '/dev1/admin',
    dev2: '/dev2/admin',
    uat: '/uat/admin',
    pro: '/pro/admin'
  }
};

export default config;
